<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Transformations & Animations Demo</title>

<style>
    body {
        font-family: Arial, sans-serif;
        text-align: center;
        background: #f2f2f2;
        padding: 40px;
    }

    h1 {
        margin-bottom: 25px;
    }

    .box {
        width: 150px;
        height: 150px;
        background: rgb(52, 152, 219);
        margin: 50px auto;
        display: flex;
        justify-content: center;
        align-items: center;
        color: white;
        font-size: 20px;
        border-radius: 10px;
        animation: rotateScale 4s infinite alternate ease-in-out;
    }

    @keyframes rotateScale {
        0% { transform: rotate(0deg) scale(1); background: #2980b9; }
        50% { transform: rotate(180deg) scale(1.3); background: #8e44ad; }
        100% { transform: rotate(360deg) scale(1); background: #16a085; }
    }
</style>
</head>
<body>

<h1>Transformations & Animations</h1>

<div class="box">Animate</div>

</body>
</html>
